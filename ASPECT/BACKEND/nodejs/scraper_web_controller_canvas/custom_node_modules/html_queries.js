exports.htmlqueries= {HTML_Query,JSLIB_Query,CSS_Query,Image_Query};
var fs = require('fs');
const { url } = require('inspector');
function HTML_Query(req, res, filepath) {
    fs.readFile(filepath, function (err, data) {
      res.writeHead(200, { 'Content-Type': 'text/html' });
      res.write(data);
      res.end();
    });
  }
  function JSLIB_Query(req, res, filepath) {
   //fs.readFile('client_ajax.js', function (err, data) {
    fs.readFile(filepath, function (err, data) {
     if(err){
      console.log("ErrorOccured");
      }
      else
      {
        res.writeHead(200, { 'Content-Type': 'text/javascript' });
        res.write(data);
        res.end();
      }
    });
  }
  function CSS_Query(req, res, filepath) {
    //fs.readFile('client_ajax.js', function (err, data) {
     fs.readFile(filepath, function (err, data) {
      if(err){
       console.log("ErrorOccured");
       }
       else
       {
         res.writeHead(200, { 'Content-Type': 'text/css' });
         res.write(data);
         res.end();
       }
     });
   }
   function Image_Query(req, res, filepath) {
    //fs.readFile('client_ajax.js', function (err, data) {
     var content_type;
    if(req.url.includes(".svg")){
      console.log("Request for svg");
      content_type='image/svg+xml';
      fs.readFile(filepath, function (err, data) {
        if(err){
         console.log("ErrorOccured: "+ err);
         }
         else
         {
           res.writeHead(200, { 'Content-Type': content_type });
           res.write(data);
           res.end();
         }
       });
    }
   }